# netlify.toml — Hello Neighbor
[build]
  publish = "."
  functions = "functions"

# Optional: basic security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "no-referrer"
